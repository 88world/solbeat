"use client";

/**
 * Removed entirely. The previous orbs created saturated rainbow halos in the
 * hero bg that clashed with the central sphere. Premium = restraint; the bg
 * is now flat, and the only colored elements are the sphere itself and the
 * ECG trace inside the MarketPulse panel.
 */
export function AmbientOrbs() {
  return null;
}
